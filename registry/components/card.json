{
  "name": "card",
  "title": "Card",
  "description": "Basiscontainer met kop, inhoud en voettekst.",
  "category": "Basis",
  "dependencies": [],
  "requires": [],
  "files": [
    {
      "path": "components/card.tsx",
      "content": "\"use client\";\nimport * as React from \"react\";\nimport { cn } from \"../lib/cn\";\n\nexport interface CardProps extends React.HTMLAttributes<HTMLDivElement> {\n  /** Hover-effect + cursor pointer (voor klikbare kaarten). */\n  interactive?: boolean;\n  /** Verwijdert de standaard binnenmarge (bv. als er een tabel in staat). */\n  flush?: boolean;\n}\n\n/** Card — basiscontainer met surface, rand en schaduw. */\nexport const Card = React.forwardRef<HTMLDivElement, CardProps>(function Card(\n  { interactive, flush, className, ...rest },\n  ref\n) {\n  return (\n    <div\n      ref={ref}\n      className={cn(\"lui-card\", interactive && \"lui-card-interactive\", flush && \"lui-card-flush\", className)}\n      {...rest}\n    />\n  );\n});\n\nexport const CardHeader = React.forwardRef<HTMLDivElement, React.HTMLAttributes<HTMLDivElement>>(\n  function CardHeader({ className, ...rest }, ref) {\n    return <div ref={ref} className={cn(\"lui-card-header\", className)} {...rest} />;\n  }\n);\n\nexport const CardTitle = React.forwardRef<HTMLHeadingElement, React.HTMLAttributes<HTMLHeadingElement>>(\n  function CardTitle({ className, ...rest }, ref) {\n    return <h3 ref={ref} className={cn(\"lui-card-title\", className)} {...rest} />;\n  }\n);\n\nexport const CardDescription = React.forwardRef<\n  HTMLParagraphElement,\n  React.HTMLAttributes<HTMLParagraphElement>\n>(function CardDescription({ className, ...rest }, ref) {\n  return <p ref={ref} className={cn(\"lui-card-description\", className)} {...rest} />;\n});\n\nexport const CardContent = React.forwardRef<HTMLDivElement, React.HTMLAttributes<HTMLDivElement>>(\n  function CardContent({ className, ...rest }, ref) {\n    return <div ref={ref} className={cn(\"lui-card-content\", className)} {...rest} />;\n  }\n);\n\nexport const CardFooter = React.forwardRef<HTMLDivElement, React.HTMLAttributes<HTMLDivElement>>(\n  function CardFooter({ className, ...rest }, ref) {\n    return <div ref={ref} className={cn(\"lui-card-footer\", className)} {...rest} />;\n  }\n);\n\n/** Actiezone rechtsboven in een CardHeader. */\nexport const CardAction = React.forwardRef<HTMLDivElement, React.HTMLAttributes<HTMLDivElement>>(\n  function CardAction({ className, ...rest }, ref) {\n    return <div ref={ref} className={cn(\"lui-card-action\", className)} {...rest} />;\n  }\n);\n"
    },
    {
      "path": "components/card.css",
      "content": "/* ============================ Card ============================ */\n.lui-card {\n  background: var(--surface);\n  border: 1px solid var(--border);\n  border-radius: var(--r-lg);\n  box-shadow: var(--sh-sm);\n  display: flex; flex-direction: column;\n  min-width: 0;\n}\n.lui-card-interactive {\n  cursor: pointer;\n  transition: border-color var(--dur), box-shadow var(--dur), transform var(--dur);\n}\n.lui-card-interactive:hover {\n  border-color: var(--border-strong); box-shadow: var(--sh-md); transform: translateY(-2px);\n}\n.lui-card-interactive:focus-visible { outline: none; box-shadow: var(--ring); }\n\n.lui-card-header {\n  display: flex; align-items: flex-start; gap: 14px;\n  padding: 20px 22px 0;\n}\n.lui-card-header > :first-child { flex: 1; min-width: 0; }\n.lui-card-title { font-size: var(--fs-lg); font-weight: 650; letter-spacing: -0.02em; color: var(--text); }\n.lui-card-description { font-size: 13.5px; color: var(--text-3); margin-top: 4px; line-height: 1.5; }\n.lui-card-action { flex: none; display: flex; align-items: center; gap: 8px; }\n.lui-card-content { padding: calc(18px * var(--density)) 22px; flex: 1; min-width: 0; }\n.lui-card-header + .lui-card-content { padding-top: 16px; }\n.lui-card-footer {\n  display: flex; align-items: center; gap: 10px;\n  padding: 0 22px 20px; margin-top: auto;\n}\n.lui-card-flush .lui-card-content { padding: 0; }\n"
    }
  ]
}
