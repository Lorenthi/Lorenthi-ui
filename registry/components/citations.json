{
  "name": "citations",
  "title": "Citations",
  "description": "Bronverwijzingen in de tekst met een lijst eronder, plus een verbruiksmeter.",
  "category": "AI",
  "dependencies": [],
  "requires": [],
  "files": [
    {
      "path": "components/citations.tsx",
      "content": "\"use client\";\nimport * as React from \"react\";\nimport { cn } from \"../lib/cn\";\nimport { Icon } from \"../icons/icon\";\n\nexport interface Citation {\n  id: string;\n  title: React.ReactNode;\n  /** Adres van de bron; maakt het bolletje en de rij klikbaar. */\n  url?: string;\n  /** Domein of bestandsnaam onder de titel. */\n  source?: React.ReactNode;\n  /** Het aangehaalde stukje tekst. */\n  snippet?: React.ReactNode;\n}\n\ninterface CitationsContextValue {\n  bronnen: Citation[];\n  actief: string | null;\n  setActief: (id: string | null) => void;\n}\n\nconst CitationsContext = React.createContext<CitationsContextValue | null>(null);\n\nexport interface CitationsProps extends React.HTMLAttributes<HTMLDivElement> {\n  sources: Citation[];\n  /** De bronnenlijst onderaan verbergen. */\n  hideList?: boolean;\n  listLabel?: React.ReactNode;\n  /** De lijst ingeklapt beginnen. */\n  collapsed?: boolean;\n}\n\n/**\n * Citations — omhulsel voor tekst met bronverwijzingen. Zet er `CitationMark`\n * in en onderaan verschijnt de bijbehorende lijst; het bolletje en de rij\n * lichten samen op.\n */\nexport const Citations = React.forwardRef<HTMLDivElement, CitationsProps>(function Citations(\n  { sources, hideList, listLabel = \"Bronnen\", collapsed, className, children, ...rest },\n  ref\n) {\n  const [actief, setActief] = React.useState<string | null>(null);\n  const [open, setOpen] = React.useState(!collapsed);\n\n  return (\n    <CitationsContext.Provider value={{ bronnen: sources, actief, setActief }}>\n      <div ref={ref} className={cn(\"lui-cites\", className)} {...rest}>\n        <div className=\"lui-cites-text\">{children}</div>\n\n        {!hideList && sources.length > 0 && (\n          <div className=\"lui-cites-list\">\n            <button\n              type=\"button\"\n              className=\"lui-cites-toggle\"\n              onClick={() => setOpen(!open)}\n              aria-expanded={open}\n            >\n              {listLabel}\n              <span className=\"lui-cites-count\">{sources.length}</span>\n              <span className=\"lui-cites-chevron\" data-open={open ? \"\" : undefined} aria-hidden=\"true\" />\n            </button>\n\n            {open && (\n              <ol className=\"lui-cites-items\">\n                {sources.map((bron, index) => {\n                  const inhoud = (\n                    <>\n                      <span className=\"lui-cites-num\">{index + 1}</span>\n                      <span className=\"lui-cites-body\">\n                        <span className=\"lui-cites-title\">{bron.title}</span>\n                        {bron.source && <span className=\"lui-cites-source\">{bron.source}</span>}\n                        {bron.snippet && <span className=\"lui-cites-snippet\">{bron.snippet}</span>}\n                      </span>\n                      {bron.url && <Icon name=\"externalLink\" size={13} className=\"lui-cites-ext\" />}\n                    </>\n                  );\n                  return (\n                    <li\n                      key={bron.id}\n                      className=\"lui-cites-item\"\n                      data-active={actief === bron.id ? \"\" : undefined}\n                      onMouseEnter={() => setActief(bron.id)}\n                      onMouseLeave={() => setActief(null)}\n                    >\n                      {bron.url ? (\n                        <a href={bron.url} target=\"_blank\" rel=\"noreferrer\" className=\"lui-cites-link\">\n                          {inhoud}\n                        </a>\n                      ) : (\n                        <span className=\"lui-cites-link\">{inhoud}</span>\n                      )}\n                    </li>\n                  );\n                })}\n              </ol>\n            )}\n          </div>\n        )}\n      </div>\n    </CitationsContext.Provider>\n  );\n});\n\nexport interface CitationMarkProps extends React.HTMLAttributes<HTMLElement> {\n  /** Verwijst naar een id uit `sources`. */\n  id: string;\n}\n\n/**\n * CitationMark — het bolletje in de lopende tekst. Zweven licht de bijhorende\n * rij in de bronnenlijst op, en omgekeerd.\n */\nexport const CitationMark = React.forwardRef<HTMLElement, CitationMarkProps>(function CitationMark(\n  { id, className, ...rest },\n  ref\n) {\n  const context = React.useContext(CitationsContext);\n  if (!context) throw new Error(\"CitationMark moet binnen <Citations> staan.\");\n\n  const index = context.bronnen.findIndex((bron) => bron.id === id);\n  if (index === -1) return null;\n  const bron = context.bronnen[index];\n\n  const inhoud = (\n    <>\n      {index + 1}\n      <span className=\"lui-cite-tip\" role=\"tooltip\">\n        <span className=\"lui-cite-tip-title\">{bron.title}</span>\n        {bron.source && <span className=\"lui-cite-tip-source\">{bron.source}</span>}\n      </span>\n    </>\n  );\n\n  const gedeeld = {\n    className: cn(\"lui-cite\", className),\n    \"data-active\": context.actief === id ? \"\" : undefined,\n    onMouseEnter: () => context.setActief(id),\n    onMouseLeave: () => context.setActief(null),\n    onFocus: () => context.setActief(id),\n    onBlur: () => context.setActief(null),\n  };\n\n  if (bron.url) {\n    return (\n      <a\n        ref={ref as React.Ref<HTMLAnchorElement>}\n        href={bron.url}\n        target=\"_blank\"\n        rel=\"noreferrer\"\n        aria-label={`Bron ${index + 1}`}\n        {...gedeeld}\n        {...rest}\n      >\n        {inhoud}\n      </a>\n    );\n  }\n\n  return (\n    <sup ref={ref as React.Ref<HTMLElement>} aria-label={`Bron ${index + 1}`} {...gedeeld} {...rest}>\n      {inhoud}\n    </sup>\n  );\n});\n\nexport interface UsageQuotaProps extends React.HTMLAttributes<HTMLDivElement> {\n  /** Wat er al verbruikt is. */\n  used: number;\n  /** Wat er in totaal mag. */\n  limit: number;\n  label?: React.ReactNode;\n  /** Eenheid achter de getallen, bv. \" berichten\". */\n  unit?: React.ReactNode;\n  /** Wanneer de teller weer op nul gaat. */\n  resetAt?: Date | string;\n  /** Vanaf welk aandeel de balk amber en rood wordt. */\n  thresholds?: [number, number];\n  /** Compacte regel zonder balk. */\n  compact?: boolean;\n  formatValue?: (value: number) => string;\n  locale?: string;\n  /** Knop rechts, bv. \"Upgraden\". */\n  action?: React.ReactNode;\n}\n\n/**\n * UsageQuota — hoeveel van een limiet er op is, wanneer hij weer aanvult, en\n * een waarschuwingskleur zodra het krap wordt.\n */\nexport const UsageQuota = React.forwardRef<HTMLDivElement, UsageQuotaProps>(function UsageQuota(\n  {\n    used,\n    limit,\n    label = \"Verbruik\",\n    unit,\n    resetAt,\n    thresholds = [0.75, 0.92],\n    compact,\n    formatValue,\n    locale = \"nl-BE\",\n    action,\n    className,\n    ...rest\n  },\n  ref\n) {\n  const deel = limit > 0 ? Math.min(Math.max(used / limit, 0), 1) : 0;\n  const toon = deel >= thresholds[1] ? \"red\" : deel >= thresholds[0] ? \"amber\" : \"accent\";\n  const opmaak = (waarde: number) =>\n    formatValue ? formatValue(waarde) : waarde.toLocaleString(locale);\n\n  const reset = React.useMemo(() => {\n    if (!resetAt) return null;\n    const datum = resetAt instanceof Date ? resetAt : new Date(resetAt);\n    if (Number.isNaN(datum.getTime())) return null;\n    return new Intl.DateTimeFormat(locale, { day: \"numeric\", month: \"short\", hour: \"2-digit\", minute: \"2-digit\" }).format(datum);\n  }, [resetAt, locale]);\n\n  return (\n    <div\n      ref={ref}\n      className={cn(\"lui-quota\", compact && \"lui-quota-compact\", className)}\n      data-tone={toon}\n      {...rest}\n    >\n      <div className=\"lui-quota-head\">\n        <span className=\"lui-quota-label\">{label}</span>\n        <span className=\"lui-quota-value\">\n          {opmaak(used)} / {opmaak(limit)}\n          {unit}\n        </span>\n      </div>\n\n      {!compact && (\n        <div\n          className=\"lui-quota-track\"\n          role=\"progressbar\"\n          aria-valuemin={0}\n          aria-valuemax={limit}\n          aria-valuenow={used}\n          aria-label={typeof label === \"string\" ? label : undefined}\n        >\n          <span className=\"lui-quota-fill\" style={{ width: `${deel * 100}%` }} />\n        </div>\n      )}\n\n      {(reset || action) && (\n        <div className=\"lui-quota-foot\">\n          {reset && (\n            <span className=\"lui-quota-reset\">\n              <Icon name=\"refresh\" size={13} />\n              Vult aan op {reset}\n            </span>\n          )}\n          {action}\n        </div>\n      )}\n    </div>\n  );\n});\n"
    },
    {
      "path": "components/citations.css",
      "content": "/* ==================== Citations & UsageQuota ==================== */\n.lui-cites { display: grid; gap: 12px; }\n.lui-cites-text { line-height: 1.7; color: var(--text); }\n\n.lui-cite {\n  position: relative; display: inline-grid; place-items: center;\n  min-width: 16px; height: 16px; padding: 0 4px; margin-inline-start: 2px;\n  border-radius: var(--r-full); background: var(--surface-3); color: var(--text-2);\n  font-size: 10.5px; font-weight: 700; line-height: 1; vertical-align: super;\n  text-decoration: none; cursor: pointer;\n  transition: background var(--dur-fast), color var(--dur-fast);\n}\n.lui-cite:hover, .lui-cite[data-active] { background: var(--accent); color: var(--accent-fg); }\n.lui-cite:focus-visible { outline: none; box-shadow: var(--ring); }\n\n.lui-cite-tip {\n  position: absolute; bottom: calc(100% + 7px); left: 50%; transform: translateX(-50%);\n  z-index: 3; display: none; width: max-content; max-width: 260px; padding: 7px 10px; text-align: start;\n  border: 1px solid var(--border); border-radius: var(--r-sm);\n  background: var(--surface); box-shadow: var(--sh-lg);\n  font-size: 12px; font-weight: 500; line-height: 1.45; color: var(--text); pointer-events: none;\n}\n.lui-cite:hover .lui-cite-tip, .lui-cite:focus-visible .lui-cite-tip { display: grid; gap: 2px; }\n.lui-cite-tip-title { font-weight: 650; }\n.lui-cite-tip-source { color: var(--text-3); }\n\n.lui-cites-list { border-top: 1px solid var(--border); padding-top: 10px; }\n.lui-cites-toggle {\n  display: flex; align-items: center; gap: 8px; padding: 0; border: 0; background: transparent;\n  font: inherit; font-size: 12.5px; font-weight: 650; color: var(--text-2); cursor: pointer;\n}\n.lui-cites-toggle:hover { color: var(--text); }\n.lui-cites-toggle:focus-visible { outline: none; box-shadow: var(--ring); }\n.lui-cites-count {\n  min-width: 18px; padding: 1px 6px; border-radius: var(--r-full);\n  background: var(--surface-3); color: var(--text-3);\n  font-size: 11px; font-weight: 650; text-align: center;\n}\n.lui-cites-chevron {\n  width: 6px; height: 6px; border-right: 1.5px solid currentColor; border-bottom: 1.5px solid currentColor;\n  transform: rotate(45deg) translate(-1px, -1px); transition: transform var(--dur-fast);\n}\n.lui-cites-chevron[data-open] { transform: rotate(225deg) translate(-1px, -1px); }\n\n.lui-cites-items { display: grid; gap: 4px; margin: 10px 0 0; padding: 0; list-style: none; counter-reset: none; }\n.lui-cites-item { list-style: none; }\n.lui-cites-link {\n  display: flex; align-items: flex-start; gap: 10px; padding: 8px 10px;\n  border: 1px solid transparent; border-radius: var(--r-sm);\n  color: inherit; text-decoration: none;\n  transition: background var(--dur-fast), border-color var(--dur-fast);\n}\na.lui-cites-link:hover, .lui-cites-item[data-active] .lui-cites-link {\n  background: var(--surface-2); border-color: var(--border);\n}\n.lui-cites-num {\n  display: grid; place-items: center; flex: none; width: 18px; height: 18px; margin-top: 1px;\n  border-radius: var(--r-full); background: var(--surface-3); color: var(--text-2);\n  font-size: 10.5px; font-weight: 700;\n}\n.lui-cites-item[data-active] .lui-cites-num { background: var(--accent); color: var(--accent-fg); }\n.lui-cites-body { display: grid; gap: 2px; min-width: 0; }\n.lui-cites-title { font-size: 13px; font-weight: 600; color: var(--text); }\n.lui-cites-source { font-size: 11.5px; color: var(--text-3); }\n.lui-cites-snippet {\n  font-size: 12.5px; color: var(--text-3); line-height: 1.5;\n  display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;\n}\n.lui-cites-ext { flex: none; margin-top: 2px; color: var(--text-3); }\n\n/* ---------- UsageQuota ---------- */\n.lui-quota { --lui-quota-color: var(--accent); display: grid; gap: 7px; width: 100%; }\n.lui-quota[data-tone=\"amber\"] { --lui-quota-color: var(--amber); }\n.lui-quota[data-tone=\"red\"] { --lui-quota-color: var(--red); }\n\n.lui-quota-head { display: flex; align-items: baseline; justify-content: space-between; gap: 12px; }\n.lui-quota-label { font-size: var(--fs-sm); color: var(--text-2); font-weight: 550; }\n.lui-quota-value {\n  font-size: var(--fs-sm); font-weight: 650; color: var(--lui-quota-color);\n  font-variant-numeric: tabular-nums;\n}\n\n.lui-quota-track { height: 7px; border-radius: var(--r-full); background: var(--surface-3); overflow: hidden; }\n.lui-quota-fill {\n  display: block; height: 100%; border-radius: var(--r-full); background: var(--lui-quota-color);\n  transition: width var(--dur-slow) var(--ease), background var(--dur) var(--ease);\n}\n\n.lui-quota-foot { display: flex; align-items: center; justify-content: space-between; gap: 10px; }\n.lui-quota-reset { display: inline-flex; align-items: center; gap: 6px; font-size: var(--fs-xs); color: var(--text-3); }\n\n@media (prefers-reduced-motion: reduce) {\n  .lui-quota-fill { transition: none; }\n}\n"
    }
  ]
}
