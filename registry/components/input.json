{
  "name": "input",
  "title": "Input",
  "description": "Tekstveld met iconen, addon, maten en foutstatus.",
  "category": "Formulieren",
  "dependencies": [
    "field"
  ],
  "requires": [],
  "files": [
    {
      "path": "components/input.tsx",
      "content": "\"use client\";\nimport * as React from \"react\";\nimport { cn } from \"../lib/cn\";\nimport { useFieldProps } from \"./field\";\n\nexport interface InputProps extends Omit<React.InputHTMLAttributes<HTMLInputElement>, \"size\" | \"prefix\"> {\n  size?: \"sm\" | \"md\" | \"lg\";\n  /** Icoon of tekst links in het veld. */\n  prefix?: React.ReactNode;\n  /** Icoon of knop rechts in het veld. */\n  suffix?: React.ReactNode;\n  /** Vaste tekst tegen het veld aan, bv. \".lorenthi.be\". */\n  addon?: React.ReactNode;\n  invalid?: boolean;\n}\n\n/** Input — tekstveld met optionele iconen, addon en foutstatus. */\nexport const Input = React.forwardRef<HTMLInputElement, InputProps>(function Input(\n  { size = \"md\", prefix, suffix, addon, invalid, className, ...props },\n  ref\n) {\n  const merged = useFieldProps(props as Parameters<typeof useFieldProps>[0]) as InputProps;\n  const { invalid: isInvalid, ...rest } = { ...merged, invalid: invalid ?? (merged as InputProps).invalid };\n\n  const input = (\n    <input\n      ref={ref}\n      className={cn(\n        \"lui-input\",\n        `lui-input-${size}`,\n        prefix && \"lui-input-has-prefix\",\n        suffix && \"lui-input-has-suffix\",\n        isInvalid && \"lui-input-invalid\",\n        !prefix && !suffix && !addon && className\n      )}\n      aria-invalid={isInvalid || undefined}\n      {...(rest as React.InputHTMLAttributes<HTMLInputElement>)}\n    />\n  );\n\n  if (!prefix && !suffix && !addon) return input;\n\n  return (\n    <div\n      className={cn(\n        \"lui-input-wrap\",\n        `lui-input-wrap-${size}`,\n        isInvalid && \"lui-input-wrap-invalid\",\n        className\n      )}\n    >\n      {prefix && <span className=\"lui-input-affix lui-input-prefix\">{prefix}</span>}\n      {input}\n      {suffix && <span className=\"lui-input-affix lui-input-suffix\">{suffix}</span>}\n      {addon && <span className=\"lui-input-addon\">{addon}</span>}\n    </div>\n  );\n});\n"
    },
    {
      "path": "components/input.css",
      "content": "/* ============================ Input ============================ */\n.lui-input {\n  width: 100%; height: calc(42px * var(--density)); padding: 0 13px;\n  background: var(--surface); color: var(--text);\n  border: 1px solid var(--border-strong); border-radius: var(--r-sm);\n  font-size: var(--fs-base); outline: none;\n  transition: border-color var(--dur-fast), box-shadow var(--dur-fast), background var(--dur-fast);\n}\n.lui-input::placeholder { color: var(--text-3); }\n.lui-input:hover:not(:disabled):not(:focus) { border-color: var(--text-3); }\n.lui-input:focus { border-color: var(--accent); box-shadow: var(--ring); }\n.lui-input:disabled { background: var(--surface-3); color: var(--text-2); cursor: not-allowed; }\n.lui-input-invalid { border-color: var(--red); }\n.lui-input-invalid:focus { border-color: var(--red); box-shadow: var(--ring-red); }\n\n.lui-input-sm { height: calc(34px * var(--density)); padding: 0 10px; font-size: 13px; border-radius: var(--r-xs); }\n.lui-input-lg { height: calc(48px * var(--density)); padding: 0 15px; font-size: var(--fs-md); border-radius: var(--r-md); }\n\n/* ---------- Wrapper met prefix/suffix/addon ---------- */\n.lui-input-wrap {\n  position: relative; display: flex; align-items: stretch; width: 100%;\n}\n.lui-input-wrap .lui-input { border-radius: var(--r-sm); }\n.lui-input-wrap-sm .lui-input { border-radius: var(--r-xs); }\n.lui-input-wrap-lg .lui-input { border-radius: var(--r-md); }\n\n.lui-input-affix {\n  position: absolute; top: 0; bottom: 0; display: flex; align-items: center;\n  color: var(--text-3); pointer-events: none; font-size: 13px;\n}\n.lui-input-affix svg { width: 16px; height: 16px; }\n.lui-input-prefix { left: 12px; }\n.lui-input-suffix { right: 12px; pointer-events: auto; }\n.lui-input-has-prefix { padding-left: 36px; }\n.lui-input-has-suffix { padding-right: 36px; }\n\n.lui-input-wrap .lui-input-addon {\n  display: inline-flex; align-items: center; padding: 0 12px;\n  background: var(--surface-3); border: 1px solid var(--border-strong); border-left: none;\n  border-radius: 0 var(--r-sm) var(--r-sm) 0;\n  color: var(--text-3); font-size: 13px; font-family: var(--mono); white-space: nowrap;\n}\n.lui-input-wrap:has(.lui-input-addon) .lui-input { border-radius: var(--r-sm) 0 0 var(--r-sm); }\n.lui-input-wrap-invalid .lui-input-addon { border-color: var(--red); }\n"
    }
  ]
}
